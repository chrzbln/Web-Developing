// pascal casing
function Message()
{
    // javascript XML
    const name = 'Chriztian';
    if (name) // if there is something in the string
        return <h1>Hello {name}</h1>; // return name
    return <h1>Hello World </h1>; // else, return hello world
}

export default Message;